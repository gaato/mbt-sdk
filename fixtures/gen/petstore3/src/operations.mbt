// GENERATED by tools/gen — DO NOT EDIT.

///|
fn[T : ToJson] parameter_value(value : T) -> String {
  match value.to_json() {
    String(value) => value
    True => "true"
    False => "false"
    Number(value, repr~) =>
      match repr {
        Some(text) => text
        None => value.to_string()
      }
    other => other.stringify()
  }
}

///|
fn percent_encode(value : String) -> String {
  let hex = b"0123456789ABCDEF"
  let output : Array[Byte] = []
  for byte in @utf8.encode(value) {
    let code = byte.to_int()
    if (code >= 65 && code <= 90) ||
      (code >= 97 && code <= 122) ||
      (code >= 48 && code <= 57) ||
      byte == b'-' ||
      byte == b'.' ||
      byte == b'_' ||
      byte == b'~' {
      output.push(byte)
    } else {
      output.push(b'%')
      output.push(hex[code >> 4])
      output.push(hex[code & 15])
    }
  }
  try! @utf8.decode(Bytes::from_array(output))
}

///|
fn append_query(url : String, name : String, value : String) -> String {
  let separator = if url.contains("?") { "&" } else { "?" }
  url + separator + percent_encode(name) + "=" + percent_encode(value)
}

///|
fn parse_json_body(body : Bytes) -> Json raise {
  @json.parse(@utf8.decode(body))
}

///|
fn[T : @json.FromJson] decode_response(
  response : @http.Response,
) -> T raise @runtime.SdkError {
  @json.from_json(parse_json_body(response.body)) catch {
    error =>
      raise @runtime.Decode(message=error.to_string(), body=response.body)
  }
}

///|
///
/// Update an existing pet.
pub fn update_pet_request(body : Pet) -> @http.Request {
  @http.Request::put("/pet").json_body(body.to_json())
}

///|
///
/// Decodes a parsed successful updatePet response.
pub fn update_pet_decode_json(value : Json) -> Pet raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful updatePet response.
pub fn update_pet_decode(
  response : @http.Response,
) -> Pet raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Add a new pet to the store.
pub fn add_pet_request(body : Pet) -> @http.Request {
  @http.Request::post("/pet").json_body(body.to_json())
}

///|
///
/// Decodes a parsed successful addPet response.
pub fn add_pet_decode_json(value : Json) -> Pet raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful addPet response.
pub fn add_pet_decode(response : @http.Response) -> Pet raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Finds Pets by status.
pub fn find_pets_by_status_request(
  status : FindPetsByStatusStatus,
) -> @http.Request {
  let mut url = "/pet/findByStatus"
  url = append_query(url, "status", parameter_value(status))
  @http.Request::get(url)
}

///|
///
/// Decodes a parsed successful findPetsByStatus response.
pub fn find_pets_by_status_decode_json(
  value : Json,
) -> Array[Pet] raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful findPetsByStatus response.
pub fn find_pets_by_status_decode(
  response : @http.Response,
) -> Array[Pet] raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Finds Pets by tags.
pub fn find_pets_by_tags_request(tags : Array[String]) -> @http.Request {
  let mut url = "/pet/findByTags"
  for value in tags {
    url = append_query(url, "tags", parameter_value(value))
  }
  @http.Request::get(url)
}

///|
///
/// Decodes a parsed successful findPetsByTags response.
pub fn find_pets_by_tags_decode_json(
  value : Json,
) -> Array[Pet] raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful findPetsByTags response.
pub fn find_pets_by_tags_decode(
  response : @http.Response,
) -> Array[Pet] raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Find pet by ID.
pub fn get_pet_by_id_request(pet_id : Int64) -> @http.Request {
  @http.Request::get("/pet/" + percent_encode(parameter_value(pet_id)))
}

///|
///
/// Decodes a parsed successful getPetById response.
pub fn get_pet_by_id_decode_json(
  value : Json,
) -> Pet raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful getPetById response.
pub fn get_pet_by_id_decode(
  response : @http.Response,
) -> Pet raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Updates a pet in the store with form data.
pub fn update_pet_with_form_request(
  pet_id : Int64,
  name? : String,
  status? : String,
) -> @http.Request {
  let mut url = "/pet/" + percent_encode(parameter_value(pet_id))
  match name {
    Some(value) => url = append_query(url, "name", parameter_value(value))
    None => ()
  }
  match status {
    Some(value) => url = append_query(url, "status", parameter_value(value))
    None => ()
  }
  @http.Request::post(url)
}

///|
///
/// Decodes a parsed successful updatePetWithForm response.
pub fn update_pet_with_form_decode_json(
  value : Json,
) -> Pet raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful updatePetWithForm response.
pub fn update_pet_with_form_decode(
  response : @http.Response,
) -> Pet raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Deletes a pet. The caller handles the response as @http.Response.
pub fn delete_pet_request(pet_id : Int64, api_key? : String) -> @http.Request {
  let mut request = @http.Request::delete(
    "/pet/" + percent_encode(parameter_value(pet_id)),
  )
  match api_key {
    Some(value) => request = request.header("api_key", parameter_value(value))
    None => ()
  }
  request
}

///|
///
/// Uploads an image.
pub fn upload_file_request(
  pet_id : Int64,
  additional_metadata? : String,
) -> @http.Request {
  let mut url = "/pet/" +
    percent_encode(parameter_value(pet_id)) +
    "/uploadImage"
  match additional_metadata {
    Some(value) =>
      url = append_query(url, "additionalMetadata", parameter_value(value))
    None => ()
  }
  @http.Request::post(url)
}

///|
///
/// Decodes a parsed successful uploadFile response.
pub fn upload_file_decode_json(
  value : Json,
) -> ApiResponse raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful uploadFile response.
pub fn upload_file_decode(
  response : @http.Response,
) -> ApiResponse raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Returns pet inventories by status.
pub fn get_inventory_request() -> @http.Request {
  @http.Request::get("/store/inventory")
}

///|
///
/// Decodes a parsed successful getInventory response.
pub fn get_inventory_decode_json(
  value : Json,
) -> Map[String, Int] raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful getInventory response.
pub fn get_inventory_decode(
  response : @http.Response,
) -> Map[String, Int] raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Place an order for a pet.
pub fn place_order_request(body : Order) -> @http.Request {
  @http.Request::post("/store/order").json_body(body.to_json())
}

///|
///
/// Decodes a parsed successful placeOrder response.
pub fn place_order_decode_json(
  value : Json,
) -> Order raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful placeOrder response.
pub fn place_order_decode(
  response : @http.Response,
) -> Order raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Find purchase order by ID.
pub fn get_order_by_id_request(order_id : Int64) -> @http.Request {
  @http.Request::get(
    "/store/order/" + percent_encode(parameter_value(order_id)),
  )
}

///|
///
/// Decodes a parsed successful getOrderById response.
pub fn get_order_by_id_decode_json(
  value : Json,
) -> Order raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful getOrderById response.
pub fn get_order_by_id_decode(
  response : @http.Response,
) -> Order raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Delete purchase order by identifier. The caller handles the response as @http.Response.
pub fn delete_order_request(order_id : Int64) -> @http.Request {
  @http.Request::delete(
    "/store/order/" + percent_encode(parameter_value(order_id)),
  )
}

///|
///
/// Create user.
pub fn create_user_request(body : User) -> @http.Request {
  @http.Request::post("/user").json_body(body.to_json())
}

///|
///
/// Decodes a parsed successful createUser response.
pub fn create_user_decode_json(
  value : Json,
) -> User raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful createUser response.
pub fn create_user_decode(
  response : @http.Response,
) -> User raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Creates list of users with given input array.
pub fn create_users_with_list_input_request(
  body : Array[User],
) -> @http.Request {
  @http.Request::post("/user/createWithList").json_body(body.to_json())
}

///|
///
/// Decodes a parsed successful createUsersWithListInput response.
pub fn create_users_with_list_input_decode_json(
  value : Json,
) -> User raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful createUsersWithListInput response.
pub fn create_users_with_list_input_decode(
  response : @http.Response,
) -> User raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Logs user into the system.
pub fn login_user_request(
  username? : String,
  password? : String,
) -> @http.Request {
  let mut url = "/user/login"
  match username {
    Some(value) => url = append_query(url, "username", parameter_value(value))
    None => ()
  }
  match password {
    Some(value) => url = append_query(url, "password", parameter_value(value))
    None => ()
  }
  @http.Request::get(url)
}

///|
///
/// Decodes a parsed successful loginUser response.
pub fn login_user_decode_json(
  value : Json,
) -> String raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful loginUser response.
pub fn login_user_decode(
  response : @http.Response,
) -> String raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Logs out current logged in user session. The caller handles the response as @http.Response.
pub fn logout_user_request() -> @http.Request {
  @http.Request::get("/user/logout")
}

///|
///
/// Get user by user name.
pub fn get_user_by_name_request(username : String) -> @http.Request {
  @http.Request::get("/user/" + percent_encode(parameter_value(username)))
}

///|
///
/// Decodes a parsed successful getUserByName response.
pub fn get_user_by_name_decode_json(
  value : Json,
) -> User raise @json.JsonDecodeError {
  @json.from_json(value)
}

///|
///
/// Decodes a successful getUserByName response.
pub fn get_user_by_name_decode(
  response : @http.Response,
) -> User raise @runtime.SdkError {
  decode_response(response)
}

///|
///
/// Update user resource. The caller handles the response as @http.Response.
pub fn update_user_request(username : String, body : User) -> @http.Request {
  @http.Request::put("/user/" + percent_encode(parameter_value(username))).json_body(
    body.to_json(),
  )
}

///|
///
/// Delete user resource. The caller handles the response as @http.Response.
pub fn delete_user_request(username : String) -> @http.Request {
  @http.Request::delete("/user/" + percent_encode(parameter_value(username)))
}
