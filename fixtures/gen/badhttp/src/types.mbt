// GENERATED by tools/gen — DO NOT EDIT.

///|
///
/// Generated string enum GetBadjsonFlavorFlavor.
pub(all) enum GetBadjsonFlavorFlavor {
  Truncated
  TrailingComma
  SingleQuotes
  Nan
  Bom
  Html
  Mislabeled
  Empty
  Unterminated
  Bigint
  DuplicateKeys
  Comments
  LeadingGarbage
  Concatenated
  Deep
  Utf16
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend GetBadjsonFlavorFlavor with Eq::{equal, not_equal}

///|
pub extend GetBadjsonFlavorFlavor with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend GetBadjsonFlavorFlavor with ToJson::{to_json}

///|
pub impl ToJson for GetBadjsonFlavorFlavor with fn to_json(self) {
  let raw = match self {
    Truncated => "truncated"
    TrailingComma => "trailing-comma"
    SingleQuotes => "single-quotes"
    Nan => "nan"
    Bom => "bom"
    Html => "html"
    Mislabeled => "mislabeled"
    Empty => "empty"
    Unterminated => "unterminated"
    Bigint => "bigint"
    DuplicateKeys => "duplicate-keys"
    Comments => "comments"
    LeadingGarbage => "leading-garbage"
    Concatenated => "concatenated"
    Deep => "deep"
    Utf16 => "utf16"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend GetBadjsonFlavorFlavor with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for GetBadjsonFlavorFlavor with fn from_json(
  value,
  path,
) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "truncated" => Some(Truncated)
        "trailing-comma" => Some(TrailingComma)
        "single-quotes" => Some(SingleQuotes)
        "nan" => Some(Nan)
        "bom" => Some(Bom)
        "html" => Some(Html)
        "mislabeled" => Some(Mislabeled)
        "empty" => Some(Empty)
        "unterminated" => Some(Unterminated)
        "bigint" => Some(Bigint)
        "duplicate-keys" => Some(DuplicateKeys)
        "comments" => Some(Comments)
        "leading-garbage" => Some(LeadingGarbage)
        "concatenated" => Some(Concatenated)
        "deep" => Some(Deep)
        "utf16" => Some(Utf16)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}

///|
///
/// Generated string enum GetSseFlavorFlavor.
pub(all) enum GetSseFlavorFlavor {
  Ok
  Stall
  Cut
  Drop
  Crlf
  Cr
  NoSpace
  Multiline
  Comments
  SplitUtf8
  WrongType
  ErrorEvent
  Big
  Resume
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend GetSseFlavorFlavor with Eq::{equal, not_equal}

///|
pub extend GetSseFlavorFlavor with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend GetSseFlavorFlavor with ToJson::{to_json}

///|
pub impl ToJson for GetSseFlavorFlavor with fn to_json(self) {
  let raw = match self {
    Ok => "ok"
    Stall => "stall"
    Cut => "cut"
    Drop => "drop"
    Crlf => "crlf"
    Cr => "cr"
    NoSpace => "no-space"
    Multiline => "multiline"
    Comments => "comments"
    SplitUtf8 => "split-utf8"
    WrongType => "wrong-type"
    ErrorEvent => "error-event"
    Big => "big"
    Resume => "resume"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend GetSseFlavorFlavor with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for GetSseFlavorFlavor with fn from_json(value, path) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "ok" => Some(Ok)
        "stall" => Some(Stall)
        "cut" => Some(Cut)
        "drop" => Some(Drop)
        "crlf" => Some(Crlf)
        "cr" => Some(Cr)
        "no-space" => Some(NoSpace)
        "multiline" => Some(Multiline)
        "comments" => Some(Comments)
        "split-utf8" => Some(SplitUtf8)
        "wrong-type" => Some(WrongType)
        "error-event" => Some(ErrorEvent)
        "big" => Some(Big)
        "resume" => Some(Resume)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}

///|
///
/// Generated string enum GetRangeFlavorFlavor.
pub(all) enum GetRangeFlavorFlavor {
  Ok
  Ignore
  AdvertiseOnly
  OffByOne
  Shifted
  SuffixAsPrefix
  FromZero
  WrongTotal
  NoContentRange
  Always206
  Value200ContentRange
  Always416
  UnknownTotal
  IfRangeIgnored
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend GetRangeFlavorFlavor with Eq::{equal, not_equal}

///|
pub extend GetRangeFlavorFlavor with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend GetRangeFlavorFlavor with ToJson::{to_json}

///|
pub impl ToJson for GetRangeFlavorFlavor with fn to_json(self) {
  let raw = match self {
    Ok => "ok"
    Ignore => "ignore"
    AdvertiseOnly => "advertise-only"
    OffByOne => "off-by-one"
    Shifted => "shifted"
    SuffixAsPrefix => "suffix-as-prefix"
    FromZero => "from-zero"
    WrongTotal => "wrong-total"
    NoContentRange => "no-content-range"
    Always206 => "always-206"
    Value200ContentRange => "200-content-range"
    Always416 => "always-416"
    UnknownTotal => "unknown-total"
    IfRangeIgnored => "if-range-ignored"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend GetRangeFlavorFlavor with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for GetRangeFlavorFlavor with fn from_json(value, path) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "ok" => Some(Ok)
        "ignore" => Some(Ignore)
        "advertise-only" => Some(AdvertiseOnly)
        "off-by-one" => Some(OffByOne)
        "shifted" => Some(Shifted)
        "suffix-as-prefix" => Some(SuffixAsPrefix)
        "from-zero" => Some(FromZero)
        "wrong-total" => Some(WrongTotal)
        "no-content-range" => Some(NoContentRange)
        "always-206" => Some(Always206)
        "200-content-range" => Some(Value200ContentRange)
        "always-416" => Some(Always416)
        "unknown-total" => Some(UnknownTotal)
        "if-range-ignored" => Some(IfRangeIgnored)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}

///|
///
/// Generated string enum GetEtagFlavorFlavor.
pub(all) enum GetEtagFlavorFlavor {
  Ok
  Weak
  Changing
  Ignore
  Always304
  Mismatch
  NoValidator304
  Unquoted
  BadDate
  Future
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend GetEtagFlavorFlavor with Eq::{equal, not_equal}

///|
pub extend GetEtagFlavorFlavor with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend GetEtagFlavorFlavor with ToJson::{to_json}

///|
pub impl ToJson for GetEtagFlavorFlavor with fn to_json(self) {
  let raw = match self {
    Ok => "ok"
    Weak => "weak"
    Changing => "changing"
    Ignore => "ignore"
    Always304 => "always-304"
    Mismatch => "mismatch"
    NoValidator304 => "no-validator-304"
    Unquoted => "unquoted"
    BadDate => "bad-date"
    Future => "future"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend GetEtagFlavorFlavor with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for GetEtagFlavorFlavor with fn from_json(value, path) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "ok" => Some(Ok)
        "weak" => Some(Weak)
        "changing" => Some(Changing)
        "ignore" => Some(Ignore)
        "always-304" => Some(Always304)
        "mismatch" => Some(Mismatch)
        "no-validator-304" => Some(NoValidator304)
        "unquoted" => Some(Unquoted)
        "bad-date" => Some(BadDate)
        "future" => Some(Future)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}

///|
///
/// Generated string enum GetCookiesFlavorFlavor.
pub(all) enum GetCookiesFlavorFlavor {
  Ok
  Echo
  Folded
  Many
  Duplicate
  OnRedirect
  Delete
  ConflictingExpiry
  BadExpires
  FarFuture
  WrongDomain
  PublicSuffix
  Domain
  PathPrefix
  NamePrefixes
  Quoted
  Utf8
  Nameless
  Huge
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend GetCookiesFlavorFlavor with Eq::{equal, not_equal}

///|
pub extend GetCookiesFlavorFlavor with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend GetCookiesFlavorFlavor with ToJson::{to_json}

///|
pub impl ToJson for GetCookiesFlavorFlavor with fn to_json(self) {
  let raw = match self {
    Ok => "ok"
    Echo => "echo"
    Folded => "folded"
    Many => "many"
    Duplicate => "duplicate"
    OnRedirect => "on-redirect"
    Delete => "delete"
    ConflictingExpiry => "conflicting-expiry"
    BadExpires => "bad-expires"
    FarFuture => "far-future"
    WrongDomain => "wrong-domain"
    PublicSuffix => "public-suffix"
    Domain => "domain"
    PathPrefix => "path-prefix"
    NamePrefixes => "name-prefixes"
    Quoted => "quoted"
    Utf8 => "utf8"
    Nameless => "nameless"
    Huge => "huge"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend GetCookiesFlavorFlavor with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for GetCookiesFlavorFlavor with fn from_json(
  value,
  path,
) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "ok" => Some(Ok)
        "echo" => Some(Echo)
        "folded" => Some(Folded)
        "many" => Some(Many)
        "duplicate" => Some(Duplicate)
        "on-redirect" => Some(OnRedirect)
        "delete" => Some(Delete)
        "conflicting-expiry" => Some(ConflictingExpiry)
        "bad-expires" => Some(BadExpires)
        "far-future" => Some(FarFuture)
        "wrong-domain" => Some(WrongDomain)
        "public-suffix" => Some(PublicSuffix)
        "domain" => Some(Domain)
        "path-prefix" => Some(PathPrefix)
        "name-prefixes" => Some(NamePrefixes)
        "quoted" => Some(Quoted)
        "utf8" => Some(Utf8)
        "nameless" => Some(Nameless)
        "huge" => Some(Huge)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}

///|
///
/// Generated string enum GetAuthFlavorFlavor.
pub(all) enum GetAuthFlavorFlavor {
  Basic
  Bearer
  Digest
  DigestSha256
  None
  BareScheme
  UnknownScheme
  Token68
  Multi
  Case
  Quoted
  Utf8
  Always401
  AcceptAny
  Forbidden
  Stale
  Proxy
  Redirect
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend GetAuthFlavorFlavor with Eq::{equal, not_equal}

///|
pub extend GetAuthFlavorFlavor with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend GetAuthFlavorFlavor with ToJson::{to_json}

///|
pub impl ToJson for GetAuthFlavorFlavor with fn to_json(self) {
  let raw = match self {
    Basic => "basic"
    Bearer => "bearer"
    Digest => "digest"
    DigestSha256 => "digest-sha256"
    None => "none"
    BareScheme => "bare-scheme"
    UnknownScheme => "unknown-scheme"
    Token68 => "token68"
    Multi => "multi"
    Case => "case"
    Quoted => "quoted"
    Utf8 => "utf8"
    Always401 => "always-401"
    AcceptAny => "accept-any"
    Forbidden => "forbidden"
    Stale => "stale"
    Proxy => "proxy"
    Redirect => "redirect"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend GetAuthFlavorFlavor with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for GetAuthFlavorFlavor with fn from_json(value, path) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "basic" => Some(Basic)
        "bearer" => Some(Bearer)
        "digest" => Some(Digest)
        "digest-sha256" => Some(DigestSha256)
        "none" => Some(None)
        "bare-scheme" => Some(BareScheme)
        "unknown-scheme" => Some(UnknownScheme)
        "token68" => Some(Token68)
        "multi" => Some(Multi)
        "case" => Some(Case)
        "quoted" => Some(Quoted)
        "utf8" => Some(Utf8)
        "always-401" => Some(Always401)
        "accept-any" => Some(AcceptAny)
        "forbidden" => Some(Forbidden)
        "stale" => Some(Stale)
        "proxy" => Some(Proxy)
        "redirect" => Some(Redirect)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}

///|
///
/// Generated string enum GetCrosshostFlavorFlavor.
pub(all) enum GetCrosshostFlavorFlavor {
  SameOrigin
  ToSubdomain
  FromSubdomain
  Boomerang
  SchemeUpgrade
  SchemeDowngrade
  PortChange
  RelativeAuthority
  Jar
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend GetCrosshostFlavorFlavor with Eq::{equal, not_equal}

///|
pub extend GetCrosshostFlavorFlavor with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend GetCrosshostFlavorFlavor with ToJson::{to_json}

///|
pub impl ToJson for GetCrosshostFlavorFlavor with fn to_json(self) {
  let raw = match self {
    SameOrigin => "same-origin"
    ToSubdomain => "to-subdomain"
    FromSubdomain => "from-subdomain"
    Boomerang => "boomerang"
    SchemeUpgrade => "scheme-upgrade"
    SchemeDowngrade => "scheme-downgrade"
    PortChange => "port-change"
    RelativeAuthority => "relative-authority"
    Jar => "jar"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend GetCrosshostFlavorFlavor with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for GetCrosshostFlavorFlavor with fn from_json(
  value,
  path,
) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "same-origin" => Some(SameOrigin)
        "to-subdomain" => Some(ToSubdomain)
        "from-subdomain" => Some(FromSubdomain)
        "boomerang" => Some(Boomerang)
        "scheme-upgrade" => Some(SchemeUpgrade)
        "scheme-downgrade" => Some(SchemeDowngrade)
        "port-change" => Some(PortChange)
        "relative-authority" => Some(RelativeAuthority)
        "jar" => Some(Jar)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}

///|
///
/// Generated string enum GetCompressFlavorFlavor.
pub(all) enum GetCompressFlavorFlavor {
  Ok
  Br
  Zstd
  Deflate
  NotCompressed
  Undeclared
  Truncated
  Corrupt
  BadCrc
  TrailingGarbage
  MultiMember
  Double
  DoubleHidden
  DeflateRaw
  UnknownCoding
  Uppercase
  XGzip
  Empty
  WrongLength
  GzipFile
  Bomb
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend GetCompressFlavorFlavor with Eq::{equal, not_equal}

///|
pub extend GetCompressFlavorFlavor with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend GetCompressFlavorFlavor with ToJson::{to_json}

///|
pub impl ToJson for GetCompressFlavorFlavor with fn to_json(self) {
  let raw = match self {
    Ok => "ok"
    Br => "br"
    Zstd => "zstd"
    Deflate => "deflate"
    NotCompressed => "not-compressed"
    Undeclared => "undeclared"
    Truncated => "truncated"
    Corrupt => "corrupt"
    BadCrc => "bad-crc"
    TrailingGarbage => "trailing-garbage"
    MultiMember => "multi-member"
    Double => "double"
    DoubleHidden => "double-hidden"
    DeflateRaw => "deflate-raw"
    UnknownCoding => "unknown-coding"
    Uppercase => "uppercase"
    XGzip => "x-gzip"
    Empty => "empty"
    WrongLength => "wrong-length"
    GzipFile => "gzip-file"
    Bomb => "bomb"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend GetCompressFlavorFlavor with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for GetCompressFlavorFlavor with fn from_json(
  value,
  path,
) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "ok" => Some(Ok)
        "br" => Some(Br)
        "zstd" => Some(Zstd)
        "deflate" => Some(Deflate)
        "not-compressed" => Some(NotCompressed)
        "undeclared" => Some(Undeclared)
        "truncated" => Some(Truncated)
        "corrupt" => Some(Corrupt)
        "bad-crc" => Some(BadCrc)
        "trailing-garbage" => Some(TrailingGarbage)
        "multi-member" => Some(MultiMember)
        "double" => Some(Double)
        "double-hidden" => Some(DoubleHidden)
        "deflate-raw" => Some(DeflateRaw)
        "unknown-coding" => Some(UnknownCoding)
        "uppercase" => Some(Uppercase)
        "x-gzip" => Some(XGzip)
        "empty" => Some(Empty)
        "wrong-length" => Some(WrongLength)
        "gzip-file" => Some(GzipFile)
        "bomb" => Some(Bomb)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}

///|
///
/// Generated untagged union GetRedirectHopsHops.
pub(all) enum GetRedirectHopsHops {
  Int(Int)
  Text(String)
} derive(Eq, @debug.Debug)

///|
pub extend GetRedirectHopsHops with Eq::{equal, not_equal}

///|
pub extend GetRedirectHopsHops with @debug.Debug::{to_repr}

///|
///
/// Encodes this union as JSON.
pub extend GetRedirectHopsHops with ToJson::{to_json}

///|
pub impl ToJson for GetRedirectHopsHops with fn to_json(self) {
  match self {
    Int(value) => Json(value)
    Text(value) => Json(value)
  }
}

///|
///
/// Decodes this union from JSON.
pub extend GetRedirectHopsHops with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for GetRedirectHopsHops with fn from_json(value, path) {
  if value is Number(_, ..) {
    let decoded : Int = @json.from_json(value, path~)
    return Int(decoded)
  }
  if value is String(_) {
    let decoded : String = @json.from_json(value, path~)
    return Text(decoded)
  }
  raise @json.JsonDecodeError(
    (path, "JSON shape does not match any GetRedirectHopsHops variant"),
  )
}

///|
///
/// Generated string enum Get402PayNetwork.
pub(all) enum Get402PayNetwork {
  BaseSepolia
  Base
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend Get402PayNetwork with Eq::{equal, not_equal}

///|
pub extend Get402PayNetwork with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend Get402PayNetwork with ToJson::{to_json}

///|
pub impl ToJson for Get402PayNetwork with fn to_json(self) {
  let raw = match self {
    BaseSepolia => "base-sepolia"
    Base => "base"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend Get402PayNetwork with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for Get402PayNetwork with fn from_json(value, path) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "base-sepolia" => Some(BaseSepolia)
        "base" => Some(Base)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}

///|
///
/// Generated string enum Get402ScenarioScenario.
pub(all) enum Get402ScenarioScenario {
  Never
  Reject
  Slow
  Crash
  BadReceipt
  Overpriced
  WrongNetwork
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend Get402ScenarioScenario with Eq::{equal, not_equal}

///|
pub extend Get402ScenarioScenario with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend Get402ScenarioScenario with ToJson::{to_json}

///|
pub impl ToJson for Get402ScenarioScenario with fn to_json(self) {
  let raw = match self {
    Never => "never"
    Reject => "reject"
    Slow => "slow"
    Crash => "crash"
    BadReceipt => "bad-receipt"
    Overpriced => "overpriced"
    WrongNetwork => "wrong-network"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend Get402ScenarioScenario with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for Get402ScenarioScenario with fn from_json(
  value,
  path,
) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "never" => Some(Never)
        "reject" => Some(Reject)
        "slow" => Some(Slow)
        "crash" => Some(Crash)
        "bad-receipt" => Some(BadReceipt)
        "overpriced" => Some(Overpriced)
        "wrong-network" => Some(WrongNetwork)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}

///|
///
/// Generated string enum Get402ScenarioNetwork.
pub(all) enum Get402ScenarioNetwork {
  BaseSepolia
  Base
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend Get402ScenarioNetwork with Eq::{equal, not_equal}

///|
pub extend Get402ScenarioNetwork with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend Get402ScenarioNetwork with ToJson::{to_json}

///|
pub impl ToJson for Get402ScenarioNetwork with fn to_json(self) {
  let raw = match self {
    BaseSepolia => "base-sepolia"
    Base => "base"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend Get402ScenarioNetwork with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for Get402ScenarioNetwork with fn from_json(value, path) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "base-sepolia" => Some(BaseSepolia)
        "base" => Some(Base)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}

///|
///
/// Generated string enum Get402BrokenFlavorFlavor.
pub(all) enum Get402BrokenFlavorFlavor {
  NotBase64
  NotJson
  NoAccepts
  EmptyAccepts
  NoExtra
  NoResource
  Version99
  DecimalAmount
  MissingHeader
  V1Body
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend Get402BrokenFlavorFlavor with Eq::{equal, not_equal}

///|
pub extend Get402BrokenFlavorFlavor with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend Get402BrokenFlavorFlavor with ToJson::{to_json}

///|
pub impl ToJson for Get402BrokenFlavorFlavor with fn to_json(self) {
  let raw = match self {
    NotBase64 => "not-base64"
    NotJson => "not-json"
    NoAccepts => "no-accepts"
    EmptyAccepts => "empty-accepts"
    NoExtra => "no-extra"
    NoResource => "no-resource"
    Version99 => "version-99"
    DecimalAmount => "decimal-amount"
    MissingHeader => "missing-header"
    V1Body => "v1-body"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend Get402BrokenFlavorFlavor with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for Get402BrokenFlavorFlavor with fn from_json(
  value,
  path,
) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "not-base64" => Some(NotBase64)
        "not-json" => Some(NotJson)
        "no-accepts" => Some(NoAccepts)
        "empty-accepts" => Some(EmptyAccepts)
        "no-extra" => Some(NoExtra)
        "no-resource" => Some(NoResource)
        "version-99" => Some(Version99)
        "decimal-amount" => Some(DecimalAmount)
        "missing-header" => Some(MissingHeader)
        "v1-body" => Some(V1Body)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}

///|
///
/// Generated string enum Get402BrokenFlavorNetwork.
pub(all) enum Get402BrokenFlavorNetwork {
  BaseSepolia
  Base
  Custom(String)
} derive(Eq, @debug.Debug)

///|
pub extend Get402BrokenFlavorNetwork with Eq::{equal, not_equal}

///|
pub extend Get402BrokenFlavorNetwork with @debug.Debug::{to_repr}

///|
///
/// Encodes this string enum as JSON.
pub extend Get402BrokenFlavorNetwork with ToJson::{to_json}

///|
pub impl ToJson for Get402BrokenFlavorNetwork with fn to_json(self) {
  let raw = match self {
    BaseSepolia => "base-sepolia"
    Base => "base"
    Custom(raw) => raw
  }
  Json(raw)
}

///|
///
/// Decodes this string enum from JSON.
pub extend Get402BrokenFlavorNetwork with @json.FromJson::{from_json}

///|
pub impl @json.FromJson for Get402BrokenFlavorNetwork with fn from_json(
  value,
  path,
) {
  @sdkjson.open_string(
    value,
    path,
    raw => {
      match raw {
        "base-sepolia" => Some(BaseSepolia)
        "base" => Some(Base)
        _ => None
      }
    },
    raw => Custom(raw),
  )
}
