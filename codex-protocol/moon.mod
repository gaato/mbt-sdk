name = "gaato/codex-protocol"

version = "0.1.0"

license = "Apache-2.0"

repository = "https://github.com/gaato/mbt-sdk"

description = "Schema-generated Codex app-server v2 protocol types and typed requests"

source = "src"

preferred_target = "wasm-gc"

import {
  "gaato/sdk-runtime@0.1.0",
}
